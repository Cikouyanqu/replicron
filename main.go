// replicron is a cron-driven, idempotent table replication tool for SQL
// databases (PostgreSQL, MySQL, SQLite, SQL Server).
package main

import "github.com/Cikouyanqu/replicron/internal/cli"

func main() { cli.Execute() }
